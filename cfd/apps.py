from django.apps import AppConfig


class CfdConfig(AppConfig):
    name = 'cfd'
 # You cna change the name of the application by changing the verbose_name value. If you change the Name on the line above you will need to make other changes in the application.
    verbose_name = 'Client Financial Database'
